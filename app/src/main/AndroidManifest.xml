<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="groupapp.cs.psu.slidingpuzzle">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/puzzlepic1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/puzzlepic1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MathGameActivity" />
        <activity android:name=".NumberActivity" />
        <activity android:name=".NumberGameActivity" />
        <activity android:name=".NumberGameAIActivity" />
        <activity android:name=".MathModeSelectActivity" />
        <activity android:name=".NunModeSelectActivity" />
        <activity android:name=".MathModeLoginActivity"/>
        <activity android:name=".ModeSelectActivity" />
        <activity android:name=".Math1playerActivity" />
        <activity android:name=".Math2playersActivity" />
        <activity android:name=".MathCutthroatActivity" />
        <activity android:name=".HighScoreActivity"/>
        <activity android:name=".MathModeRegisterActivity"/>

    </application>

</manifest>